<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="interview.offer.mapper.OfferMapper">

    <sql id="candidateOfferColumns">
        o.id,
        o.enterprise_id,
        o.application_id,
        j.title AS job_title,
        o.title AS offer_title,
        o.salary_min,
        o.salary_max,
        o.currency,
        o.planned_start_date,
        o.expires_at,
        o.content,
        o.status,
        o.version,
        o.sent_at,
        o.created_at
    </sql>

    <select id="pageCandidateOffers"
            resultType="interview.offer.model.bo.CandidateOfferQueryBO">
        SELECT
        <include refid="candidateOfferColumns"/>
        FROM offers o
        LEFT JOIN job_applications ja ON ja.id = o.application_id
        LEFT JOIN jobs j ON j.id = ja.job_id
        WHERE o.candidate_user_id = #{candidateUserId}
          AND o.status &lt;&gt; 'DRAFT'
        <if test="status != null">
            AND o.status = #{status}
        </if>
        ORDER BY o.created_at
        <choose>
            <when test="ascending">ASC</when>
            <otherwise>DESC</otherwise>
        </choose>, o.id
        <choose>
            <when test="ascending">ASC</when>
            <otherwise>DESC</otherwise>
        </choose>
    </select>

    <select id="getCandidateOfferDetail"
            resultType="interview.offer.model.bo.CandidateOfferQueryBO">
        SELECT
        <include refid="candidateOfferColumns"/>
        FROM offers o
        LEFT JOIN job_applications ja ON ja.id = o.application_id
        LEFT JOIN jobs j ON j.id = ja.job_id
        WHERE o.id = #{offerId}
          AND o.candidate_user_id = #{candidateUserId}
          AND o.status &lt;&gt; 'DRAFT'
    </select>

</mapper>
