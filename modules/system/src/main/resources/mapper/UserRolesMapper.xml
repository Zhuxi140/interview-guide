<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="interview.system.rbac.mapper.UserRolesMapper">

    <select id="getUserRoles" resultType="interview.system.rbac.model.vo.UserRoleItemVO">
        SELECT
            sr.id,
            sr.role_code,
            sr.role_name,
            sr.role_scope
        FROM sys_user_roles sur
        LEFT JOIN sys_roles sr ON sur.role_id = sr.id
        WHERE sur.user_id = #{userId}
    </select>
</mapper>
